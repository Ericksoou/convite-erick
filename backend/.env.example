# ── Servidor ──────────────────────────────────────────────────
PORT=3001

# ── Segurança ─────────────────────────────────────────────────
# Senha do painel administrativo (será armazenada com hash bcrypt)
ADMIN_PASSWORD=erick18

# Segredo da sessão — use uma string longa e aleatória em produção
SESSION_SECRET=troque-por-uma-string-secreta-longa-aqui-2026

# ── CORS ──────────────────────────────────────────────────────
# URL do frontend em produção (deixe * para dev local)
FRONTEND_URL=*

# ── Ambiente ──────────────────────────────────────────────────
NODE_ENV=development
